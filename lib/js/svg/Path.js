var Path = null;
/*
var Path = _class("Path", {
  parent: Shape,

  methods: {
    init: function (id, data) {
      this.__super__().init.call(this, id, 'path', {
        stroke: '#000',
        fill: '#6F6',
        path: data
      });
    },

    style: function(st) {
      this.__super__().style.call(this, st);

      if (st.hasOwnProperty('path')) {
        this.path(st.path);
      }

      return this._style;
    },

    path: function(d) {
      if (d) {
        this._style.path = d;
        data = this.convert_data(data);
        this.add_attr({d: data});
      }
      return this._style.path;
    },

    convert_data: function(data) {
      if (data.match(/[A|a]/) !== null) syntax_error();
      return data;
    }

    get_gravity: function() {
    },

    rotate: function() {
    },

    scale: function() {
    },

    translate: function() {
    },

    reset: function() {
    }
  }
});
*/