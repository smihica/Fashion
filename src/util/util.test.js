/*
var x = 0;
var y = 0;
console.log("x:" + x + ",y:" + y);
var mtx = (new Lib.Matrix());
mtx.set({translate: {x: 10, y: 10}});
var nx = mtx.x(x,y);
var ny = mtx.y(x,y);
console.log("xn:" + nx + ",ny:" + ny);
*/

// Util.Style.convertStrokeStringTest();
// Util.Style.convertColorStringTest();